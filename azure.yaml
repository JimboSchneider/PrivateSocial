# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: private-social
services:  
  app:
    language: dotnet
    project: ./PrivateSocial.AppHost/PrivateSocial.AppHost.csproj
    host: containerapp
